sdk-version: 1.17.1
name: setup
source: daml
init-script: Main:setup
parties:
  - Alice
  - Bob
version: 0.0.1
init-script: Main:setup
start-navigator: false
sandbox-options:
  - --ledgerid=token
  - -w
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
  - ../token/.daml/dist/token-0.0.1.dar
